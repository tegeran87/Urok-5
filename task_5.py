% Базовый случай: сумма элементов пустого списка равна 0.
sum_list([], 0).

% Рекурсивный случай: сумма элементов списка равна сумме головы и суммы хвоста.
sum_list([Head | Tail], Sum) :-
    sum_list(Tail, TailSum),
    Sum is Head + TailSum.

% Пример использования:
% Вызываем sum_list с вашим целочисленным массивом в качестве аргумента.
% Например, sum_list([1, 2, 3, 4, 5], Result) вернет Result = 15.